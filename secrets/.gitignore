# Runtime secrets: NEVER commit these files.
# Populate with: scripts/generate-gateway-secrets.sh
*.key
*.pem
# Admin SHA-256 fingerprint allowlist (mounted as a Docker secret).
admin-cert-fingerprints.txt
*.txt
